# Copy to .env.local for local development
# In production this is set via the K8s frontend-deployment env vars

# Backend API base URL (leave empty to use Vite dev proxy)
VITE_API_BASE_URL=http://localhost:8000
